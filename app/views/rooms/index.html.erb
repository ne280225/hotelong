<h1>お部屋を選ぶ</h1>
<div id='menu'>
<%= menu_link_to "お部屋を選ぶ", rooms_path %> &nbsp; <%= menu_link_to "プランを知る", plans_path %>
</div>

<p><%= current_member.name %></p>

<ul>
	<% @rooms.each do |room| %>
	<li>
		<%= link_to room.room_number, room_path(room.id) %>
	</li>
	<% end %>
</ul>
